<h1> Update Swap: "<%="#{@swap.title}" %>"</h1>
<%= form_for @swap do |f|%>
    
<%=f.label :title%>
<%=f.text_field :title%>

<%= f.hidden_field :seed_id%>
<br>
<br>

<%= f.collection_select :seed_id, Seed.all, :id, :name %>
<%# first option: select seed category; second option: seeds filtered by category%>
<%# f.select :seed_id, Seed.seed_category%>
<%# f.collection_select :seed_id, Seed.fruits, :id, :name %>
<%= f.hidden_field :user_id%>

<%=f.submit "Update"%>

<%end%>


